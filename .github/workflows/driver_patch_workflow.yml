name: Patch Updates For Driver Android Workflow

on:
  push:
    branches: [main]
    paths:
      - 'apps/driver/**'
      - 'packages/**'
  workflow_dispatch:

concurrency:
  group: patch-driver
  cancel-in-progress: true

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup Shorebird
        uses: shorebirdtech/setup-shorebird@v1
        with:
          cache: true

      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'
          cache-dependency-path: apps/driver/android/*.gradle*

      - name: Setup env
        env:
          ROOT_ENV: ${{ secrets.ROOT_ENV_PROD_JSON }}
          DRIVER_KEYS: ${{ secrets.DRIVER_ENV_PROD_JSON }}
        run: |
          set -euo pipefail
          printf '%s' "$ROOT_ENV" > env.prod.json
          printf '%s' "$DRIVER_KEYS" > apps/driver/env.keys.json

      - name: Setup signing
        working-directory: apps/driver/android
        env:
          KEYSTORE_BASE64: ${{ secrets.DRIVER_KEYSTORE_BASE64 }}
          STORE_PASSWORD: ${{ secrets.DRIVER_STORE_PASSWORD }}
          KEY_PASSWORD: ${{ secrets.DRIVER_KEY_PASSWORD }}
          KEY_ALIAS: ${{ secrets.DRIVER_KEY_ALIAS }}
        run: |
          set -euo pipefail
          printf '%s' "$KEYSTORE_BASE64" | base64 --decode > app/keystore.p12
          cat > key.properties <<EOF
          storeFile=keystore.p12
          storePassword=$STORE_PASSWORD
          keyPassword=$KEY_PASSWORD
          keyAlias=$KEY_ALIAS
          EOF

      - name: Setup maps key
        working-directory: apps/driver/android
        env:
          MAPS_API_KEY: ${{ secrets.DRIVER_MAPS_API_KEY }}
        run: |
          set -euo pipefail
          printf 'MAPS_API_KEY=%s\n' "$MAPS_API_KEY" > local.properties

      - name: Verify
        working-directory: apps/driver
        run: |
          set -euo pipefail
          ls -la ../../env.prod.json env.keys.json
          ls -la android/key.properties android/local.properties android/app/keystore.p12
          cat shorebird.yaml

      - name: Shorebird Patch Android (Driver)
        working-directory: apps/driver
        env:
          SHOREBIRD_TOKEN: ${{ secrets.SHOREBIRD_TOKEN }}
        run: |
          set -euo pipefail
          shorebird patch android \
            --flavor production -t lib/main_prod.dart \
            --release-version latest \
            --no-confirm \
            -- --dart-define-from-file=../../env.prod.json \
               --dart-define-from-file=env.keys.json
